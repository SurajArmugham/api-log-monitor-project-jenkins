This project demonstrates the same monitoring workflow
implemented using Jenkins instead of GitHub Actions.

Pipeline Trigger Strategy
Push → CI validation
Pull Request → CI + CD execution

Testing Jenkins PR pipeline